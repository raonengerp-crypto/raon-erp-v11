import {store,won} from '../shared/state.js';
export function renderDashboard(el){const s=store.sites;const total=s.reduce((a,b)=>a+b.contract,0),paid=s.reduce((a,b)=>a+b.paid,0),misu=total-paid;el.innerHTML=`<div class="grid kpis"><div class="card"><h3>진행 중 현장</h3><div class="num">${s.length} 개</div><div class="sub">전체 현장</div></div><div class="card"><h3>오늘 일정</h3><div class="num">6 건</div><div class="sub">등록 일정</div></div><div class="card"><h3>미수금</h3><div class="num" style="color:var(--red)">${won(misu)}</div></div><div class="card"><h3>입금 예정</h3><div class="num">24,500,000 원</div></div></div><br><div class="grid layout-2"><section class="card"><div class="row" style="justify-content:space-between"><h2>이번 달 일정</h2><button class="btn">일정 등록</button></div><div class="calendar">${Array.from({length:35},(_,i)=>`<div class="day"><b>${i+1}</b>${[8,19,23,28].includes(i)?'<div class="event">기계설치<br>09:00~12:00</div>':''}</div>`).join('')}</div></section><aside class="grid"><div class="card"><h2>빠른 이동</h2><div class="form-grid"><button class="btn" onclick="go('field')">현장 등록</button><button class="btn ghost" onclick="go('quote')">견적서 작성</button><button class="btn ghost" onclick="go('material')">자재 등록</button><button class="btn ghost" onclick="go('tax')">세무관리</button></div></div><div class="card"><h2>계약 / 입금 현황</h2><p>계약금액 ${won(total)}</p><p>입금완료 ${won(paid)}</p><p>미수금 ${won(misu)}</p></div><div class="card"><h2>비용 / 손익 현황</h2><p>예상 순이익 ${won(total-s.reduce((a,b)=>a+b.cost,0))}</p></div></aside></div>`;}
